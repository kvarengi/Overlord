from setuptools import setup, find_packages

setup(
    name='moonriver',
    version='0.1.0',
    description='Лунная Река: резонатор на частоте Шумана, сферический солитон, размер Луны',
    author='Your Name',
    packages=find_packages(),
    python_requires='>=3.7',
    install_requires=[],
)